---
phase: 04-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lua/config/plugins/conform.lua, lua/config/plugins/mason.lua, lua/config/plugins/treesitter.lua, lua/config/plugins/lualine.lua, .planning/STATE.md]
autonomous: true
requirements: [CORE-03, EDIT-02]
---

<objective>
Finalize auto-formatting and optimize startup time to reach the < 100ms goal.

Purpose: Peak performance and consistent code quality.
Output: Configured conform.nvim, mason-tool-installer.nvim, and optimized lazy-loading.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lua/config/plugins/conform.lua
@lua/config/plugins/mason.lua
@lua/config/plugins/treesitter.lua
@lua/config/plugins/lualine.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize Auto-formatting</name>
  <files>lua/config/plugins/conform.lua, lua/config/plugins/mason.lua</files>
  <action>
    - Update `lua/config/plugins/conform.lua`:
      - Add `format_on_save` (with `lsp_fallback = true` and `timeout_ms = 500`).
      - Ensure `python` has a formatter (e.g., `ruff_format` or `black`).
    - Add `WhoIsSethDaniel/mason-tool-installer.nvim` as a plugin in `lua/config/plugins/mason.lua`.
    - Configure it to install `stylua`, `yamlfmt`, `fprettify`, and `ruff` (per requirements).
  </action>
  <verify>
    - Open a Lua file, make a change, and save. Verify `stylua` formats it.
    - Run `:Mason` and verify tool installer is active.
  </verify>
  <done>Auto-formatting is enabled on save and required tools are auto-installed.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize Plugin Loading</name>
  <files>lua/config/plugins/treesitter.lua, lua/config/plugins/lualine.lua</files>
  <action>
    - Review all plugins currently set to `lazy = false`.
    - Change `nvim-treesitter` to `event = { "BufReadPost", "BufNewFile" }` (remove `lazy = false`).
    - Change `lualine.nvim` to `event = "VeryLazy"` (remove `lazy = false`).
    - Ensure `tokyonight.nvim` remains `lazy = false` with `priority = 1000`.
    - Add `priority = 1000` to `snacks.nvim` if not already there, and keep `lazy = false`.
  </action>
  <verify>
    - Start Neovim and run `:Lazy`.
    - Verify Treesitter and Lualine are lazy-loaded on their respective events.
  </verify>
  <done>Plugin loading events are tuned for faster startup.</done>
</task>

<task type="auto">
  <name>Task 3: Profile Startup and Document Stability</name>
  <files>.planning/STATE.md</files>
  <action>
    - Run Neovim startup profiling: `nvim --startuptime startuptime.txt +q`.
    - Check the total startup time in `startuptime.txt`.
    - If startup is > 100ms, identify slow plugins and consider further lazy-loading or disabling.
    - Update `.planning/STATE.md` with current performance metrics (Startup Time, Plugin Count).
    - Document that Treesitter query errors and LSP modularity are stable (per Phase 4 polish requirements).
  </action>
  <verify>
    - `cat startuptime.txt` shows total time < 100ms.
    - `.planning/STATE.md` is updated.
  </verify>
  <done>Startup performance verified and state documented.</done>
</task>

</tasks>

<verification>
- Neovim startup < 100ms.
- Auto-formatting works on save.
- All plugins lazy-loaded where possible.
</verification>

<success_criteria>
- Reported startup time consistently < 100ms.
- Formatting on save is functional.
</success_criteria>

<output>
After completion, create `.planning/phases/04-optimization/04-01-SUMMARY.md`
</output>
